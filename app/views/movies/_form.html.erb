<%= form_with(model: movie) do |form| %>
  <% if movie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :names, "Movie Names", style: "display: block" %>
    <%= form.text_area :names, placeholder: "Enter one movie name per line" %>
  </div>

  <div>
    <%= form.label :genre, style: "display: block" %>
    <%= form.select :genre, Movie.rogers_order %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

